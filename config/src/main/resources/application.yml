server.port: ${CONFIG_PORT:8888}

eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: config
  cloud:
    config.server:
      git.uri: https://github.com/nortthon/config-repo